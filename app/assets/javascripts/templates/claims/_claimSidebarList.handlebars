{{#each claim in controller }}
	{{#linkTo 'claim' claim }}
		<li class="sidebar_item full_box">
			<span>{{ claim.claimSeq }}</span>
			<ul class="actions">
				{{#if claim.isLocked }}
					<li class="icon small_icon lock"></li>
				{{/if}}
			</ul>
		</li>
	{{/linkTo}}
{{/each}}